import { HighlitedPipesPipe } from './highlited-pipes.pipe';

describe('HighlitedPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlitedPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
